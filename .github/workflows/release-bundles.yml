name: release-bundles

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      release_type:
        description: Semver bump to publish from the latest release tag
        default: patch
        required: false
        type: choice
        options:
          - patch
          - minor
          - major

concurrency:
  group: release-bundles-main
  cancel-in-progress: false

jobs:
  build-release-bundles:
    name: build-release-bundles
    runs-on: macos-15
    outputs:
      artifact_name: ${{ steps.bundle-version.outputs.artifact_name }}
      build_version: ${{ steps.bundle-version.outputs.build_version }}
      commit_sha: ${{ steps.bundle-version.outputs.commit_sha }}
      is_release: ${{ steps.bundle-version.outputs.is_release }}
      release_page_url: ${{ steps.bundle-version.outputs.release_page_url }}
      release_tag: ${{ steps.bundle-version.outputs.release_tag }}
      release_zip_name: ${{ steps.bundle-version.outputs.release_zip_name }}
      release_zip_url: ${{ steps.bundle-version.outputs.release_zip_url }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && 'main' || github.sha }}
          fetch-depth: 0
      - uses: jdx/mise-action@v4
        with:
          version: 2026.4.18
          install: false
          cache: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - run: mise install
      - run: mise run gha-select-xcode
      - run: mise run doctor
      - id: bundle-version
        name: Compute bundle version
        env:
          RELEASE_TYPE: ${{ inputs.release_type }}
        run: mise run release-bundle-metadata
      - name: mise run release-ci
        env:
          AEROSHIFT_BUILD_VERSION: ${{ steps.bundle-version.outputs.build_version }}
        run: mise run release-ci
      - name: Upload xcodebuild log
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: xcodebuild-log-${{ steps.bundle-version.outputs.build_version }}
          path: .release/xcodebuild.log
          if-no-files-found: ignore
          retention-days: 14
      - name: Upload release bundle artifacts
        if: ${{ success() }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.bundle-version.outputs.artifact_name }}
          path: |
            .release/${{ steps.bundle-version.outputs.release_zip_name }}
          if-no-files-found: error
          retention-days: 14
  publish-release-bundles:
    name: publish-release-bundles
    runs-on: macos-15
    needs: build-release-bundles
    if: ${{ needs.build-release-bundles.result == 'success' && needs.build-release-bundles.outputs.is_release == 'true' }}
    # Configure required reviewers for the "release-bundles" environment in
    # repository settings to require a human approval before publication.
    environment:
      name: release-bundles
      url: ${{ needs.build-release-bundles.outputs.release_page_url }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.build-release-bundles.outputs.commit_sha }}
      - uses: jdx/mise-action@v4
        with:
          version: 2026.4.18
          install: false
          cache: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - run: mise install
      - name: Download release bundle artifacts
        uses: actions/download-artifact@v5
        with:
          name: ${{ needs.build-release-bundles.outputs.artifact_name }}
          path: .release
      - name: Publish release zip
        env:
          AEROSHIFT_RELEASE_ASSETS: |
            .release/${{ needs.build-release-bundles.outputs.release_zip_name }}
          AEROSHIFT_RELEASE_TAG: ${{ needs.build-release-bundles.outputs.release_tag }}
          AEROSHIFT_RELEASE_TARGET: ${{ needs.build-release-bundles.outputs.commit_sha }}
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: mise run release-publish
      - name: Generate release casks
        env:
          AEROSHIFT_BUILD_VERSION: ${{ needs.build-release-bundles.outputs.build_version }}
          AEROSHIFT_RELEASE_ZIP_SHA_SOURCE: .release/${{ needs.build-release-bundles.outputs.release_zip_name }}
          AEROSHIFT_RELEASE_ZIP_URI: ${{ needs.build-release-bundles.outputs.release_zip_url }}
        run: mise run release-casks-github-url
      - name: Publish release casks
        env:
          AEROSHIFT_RELEASE_ASSETS: |
            .release/aeroshift.rb
            .release/aeroshift-dev.rb
          AEROSHIFT_RELEASE_TAG: ${{ needs.build-release-bundles.outputs.release_tag }}
          AEROSHIFT_RELEASE_TARGET: ${{ needs.build-release-bundles.outputs.commit_sha }}
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: mise run release-publish
      - name: Publish Homebrew tap cask
        # HOMEBREW_TAP_GH_TOKEN must have contents:write on
        # Boredphilosopher96/homebrew-aeroshift.
        env:
          AEROSHIFT_HOMEBREW_TAP_COMMIT_MESSAGE: Update Homebrew casks for ${{ needs.build-release-bundles.outputs.release_tag }}
          AEROSHIFT_HOMEBREW_TAP_CASK_FILES: .release/aeroshift.rb
          AEROSHIFT_RELEASE_TAG: ${{ needs.build-release-bundles.outputs.release_tag }}
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_GH_TOKEN }}
        run: mise run release-publish-homebrew-tap
